import React from 'react';
// import { useSelector } from 'react-redux';

export default function Profile() {
  // // 이렇게 받아온 값은 컴포넌트 취급. 리렌더링 됨.
  // const weight = useSelector((state) => state);

  return (
    <>
      <h1>프로필 페이지 입니다!</h1>
    </>
  );
}
